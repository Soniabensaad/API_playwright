class BankAccount{
    constructor(owner, balance=0){
        this.owner = owner;
        this.balance = balance;

    }
    deposit(amount){
        this.balance.push(amount);
    }
    withdraw(amount){
        this.balance.filter(amount=> amount =(this.withdraw(amount)- this.deposit(amount)){
            if (this.withdraw(amount)> this.deposit(amount)) {
            console.log(`Error`);


        } 
        })
    }
}
getBalance()
